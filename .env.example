# Server-side only. Never put keys in VITE_ variables or commit a real .env.
NEBIUS_API_KEY=
# Set to an NVIDIA open model shown in your Token Factory model catalog.
NEBIUS_MODEL=
# Disabled by default. Enable only after credits and billing are verified.
ENABLE_NEBIUS=0
MAX_MODEL_CALLS=20
PORT=5188
